#include<iostream>

int main(){

   // arithmetic           ->       +  -  *  %  /
   // realtional           ->       ==  >=  <=  >  <  != 
   // logical              ->       &&  !  ||
   // Bitwise              ->       &  ^  ~  <<  >>
   // Assignment           ->       =  +=  -=  *=  %=  /=  ^=  <<=  >>=
   // increment            ->       ++
   // decrement            ->       --
   // conditional          ->       (condition)? true : false ;
   // comma                ->       ,
   // pointer              ->        *(value at address), &(address)
   // sizeof               ->        sizeof(a)
   // typecast             ->        static_cast, dynamic_cast, const_cast, reinterpret_cast
   // scope resolution     ->        ::
   // member access op     ->         .   ->
   // new and delete       ->         new,  delete  
  

    return 0;
}